services:
  app:
    build: .
    container_name: gp02-app
    volumes:
      - .:/app                      # ソースコードをマウント
      - node_modules:/app/node_modules  # node_modules は名前付きボリュームで高速化
    working_dir: /app
    ports:
      - "3000:3000"
    command: sleep infinity         # Dev Container 用に待機させる
    depends_on:
      - db                          # db を先に起動する

  db:
    image: postgres:18
    container_name: gp02-db
    environment:
      POSTGRES_USER: gp02_user       # 初回起動時に作られるユーザー
      POSTGRES_PASSWORD: gp02_pass   # そのパスワード(ローカル開発専用)
      POSTGRES_DB: gp02_db    # 初回起動時に作られるデータベース名
    volumes:
      - db_data:/var/lib/postgresql  # データを永続化(18+ は /data を付けない)

volumes:
  node_modules:
  db_data: